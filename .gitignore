__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Fetched and built audio is not committed…
samples/
build/
*.wav

# …except loops/, which the published site serves. Small (3.3 MB), derived from
# CC0 source, and without them there is no player to link to. Note `loops/`
# itself must not be ignored, or the re-include below could not reach into it.
!loops/*.wav
tmp/
vendor/
.playwright-mcp/
